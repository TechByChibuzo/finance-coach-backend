name: Deploy to AWS ECS

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1

      - name: Login to ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build and push Docker image
        run: |
          docker buildx create --use
          docker buildx build \
            --platform linux/amd64 \
            --push \
            -t 516737672838.dkr.ecr.us-east-1.amazonaws.com/finance-coach-backend:latest \
            .

      - name: Deploy to ECS
        run: |
          REVISION=$(aws ecs describe-task-definition \
            --task-definition finance-coach-backend \
            --query 'taskDefinition.revision' \
            --output text)
          aws ecs update-service \
            --cluster finance-coach-cluster \
            --service finance-coach-service \
            --task-definition finance-coach-backend:$REVISION \
            --force-new-deployment

      - name: Wait for deployment
        run: |
          aws ecs wait services-stable \
            --cluster finance-coach-cluster \
            --services finance-coach-service